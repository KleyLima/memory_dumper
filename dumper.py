# -*- coding: utf-8 -*-

from source.ram import RAM

# Creates a RAM instance, its all inherited inside this class.
ram = RAM()
print("Use the ram object to keep using the program.")
print("Type 'ram' to see the data fetch until now and ram.new_file to append a new file")
